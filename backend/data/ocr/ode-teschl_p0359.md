$$\mathbb{N} = \{1, 2, 3, \ldots\}$$ the set of positive integers
$$\mathbb{N}_0 = \mathbb{N} \cup \{0\}$$
$$o(.)$$ ...Landau symbol
$$O(.)$$ ...Landau symbol
$$\Omega(f)$$ ...set of nonwandering points, 196
$$P_{\Sigma}(y)$$ ...Poincaré map, 197
$$\text{Per}(f) = \{x | f(x) = x\}$$ set of periodic points of $f$, 282
$$\Phi(t, x_0)$$ ...flow of a dynamical system, 189
$$\Pi(t, t_0)$$ ...principal matrix of a linear system, 81
$$\mathbb{R}$$ ...the set of reals
$$\text{Ran}(A)$$ ...range of a matrix
$$\sigma$$ ...shift map on $\Sigma_N$, 303
$$\sigma(A)$$ ...spectrum (set of eigenvalues) of a matrix
$$\Sigma_N$$ ...sequence space over $N$ symbols, 302
$$\text{sign}(x)$$ ...+1 for $x > 0$ and $-1$ for $x < 0$; sign function
$$T_{\pm}(x)$$ ...positive, negative lifetime of $x$, 192
$$T(x)$$ ...period of $x$ (if $x$ is periodic), 192
$$T_{\mu}$$ ...tent map, 297
$$\omega_{\pm}(x)$$ ...positive, negative $\omega$-limit set of $x$, 193
$$W^{\pm}$$ ...(un)stable set, 255, 231, 282
$$\mathbb{Z}$$ = {..., $-2$, $-1$, $0$, $1$, $2$, ...} the set of integers
$$z$$ ...a complex number
$$\sqrt{z}$$ ...square root of $z$ with branch cut along $(-\infty, 0)$
$$z^*$$ ...complex conjugation
$$\|\cdot\|$$ ...norm in a Banach space
$$|.\|$$ ...Euclidean norm in $\mathbb{R}^n$ respectively $\mathbb{C}^n$
$$\langle.,..\rangle$$ ...scalar product in $\mathfrak{H}_0$, 146
$$(\lambda_1, \lambda_2) = \{\lambda \in \mathbb{R} | \lambda_1 < \lambda < \lambda_2\}$, open interval
$$[\lambda_1, \lambda_2] = \{\lambda \in \mathbb{R} | \lambda_1 \leq \lambda \leq \lambda_2\}$, closed interval
$$\lfloor x \rfloor = \max\{n \in \mathbb{Z} | n \leq x\}$, floor function
$$\lceil x \rceil = \min\{n \in \mathbb{Z} | n \geq x\}$, ceiling function
$$a \wedge b = \text{cross product in } \mathbb{R}^3$$