$$\cos(\theta - \varphi) = -\frac{1}{e}, \quad \cos(\theta - \varphi) = \frac{1}{e}$$

Figura 7.3:

$$r_1(\theta) = \frac{ed}{1 - e \cos(\theta - \varphi)}, \quad \cos(\theta - \varphi) < \frac{1}{e};$$

o segundo é parametrizado por

$$r_2(\theta) = \frac{-ed}{1 + e \cos(\theta - \varphi)}, \quad \cos(\theta - \varphi) < -\frac{1}{e}.$$

Uma alternativa, para o segundo ramo, é usar a região correspondente a $\cos(\theta - \varphi) > 1/e$, observando que $\theta \mapsto \theta + \pi$ é uma bijeção entre essa região e a definida por $\cos(\theta - \varphi) < -1/e$, mantendo os valores absolutos dos cossenos, mas invertendo-lhes os sinais. Lançando mão de valores negativos para $r$, o mesmo ramo pode ser descrito por

$$r_2(\theta) = \frac{ed}{1 - e \cos(\theta - \varphi)}, \quad \cos(\theta - \varphi) > \frac{1}{e}.$$

Como a nova versão para $r_2$ nos dá a mesma fórmula que já usávamos para $r_1$, podemos juntá-las em uma só, parametrizando a hipérbole (os dois ramos incluídos) por