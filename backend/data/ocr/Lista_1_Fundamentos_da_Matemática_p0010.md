g) Em $\mathbb{R}$, $x \sim y$ se $x - y \in \mathbb{Z}$.

h) Em $\mathbb{R}$, $x \sim y$ se $\lfloor x \rfloor = \lfloor y \rfloor$, onde $\lfloor x \rfloor := maior inteiro menor ou igual à $x$.

i) Em $\mathbb{R}$, $x \sim y$ se $|x - y| < 1$.

j) Em $\mathbb{Z}$, $a \sim b$ se $a^2 = b^2$.

k) Em $\mathbb{Z}$, $a \sim b$ se $a$ divide $b$.

l) Em $\mathbb{R}$, $x \sim y$ se $x - y \in \mathbb{Q}$.

m) Em $\mathbb{R}^2$, $(x, y) \sim (u, v)$ se $x + v = y + u$.

**Exercício 39.** Para cada relação de equivalência abaixo, determine as classes de equivalência e descreva o conjunto quociente.

a) Em $\mathbb{Z}$, $a \sim b$ se $a - b$ é múltiplo de 2.

b) Em $\mathbb{Z}$, $a \sim b$ se $a - b$ é múltiplo de 3.

c) Em $\mathbb{Z}$, $a \sim b$ se $a - b$ é múltiplo de 5.

d) Em $\mathbb{R}$, $x \sim y$ se $x - y \in \mathbb{Z}$.

e) Em $\mathbb{R}$, $x \sim y$ se $\lfloor x \rfloor = \lfloor y \rfloor$, onde $\lfloor x \rfloor := maior inteiro menor ou igual à $x$.

f) Em $\mathbb{Z}$, $a \sim b$ se $a^2 = b^2$.

**Exercício 40.** Seja $\sim$ uma relação de equivalência em um conjunto $X$. Mostre que as classes de equivalência de $\sim$ constituem uma partição de $X$, isto é, uma família de subconjuntos não vazios, dois a dois disjuntos, cuja união é $X$.

**Exercício 41.** Em cada item, determine se a relação $\prec$ no conjunto indicado é reflexiva, antissimétrica e transitiva. Em caso afirmativo, diga se $\prec$ define uma ordem parcial ou total.

a) Em $\mathbb{Z}$, $a \prec b$ se $a \leq b$.

b) Em $\mathbb{Z}$, $a \prec b$ se $a < b$.

c) Em $\mathbb{Z}$, $a \prec b$ se $a \mid b$.

d) Em $\mathbb{N}$, $a \prec b$ se $b \mid a$.

e) Em $\mathbb{Z}$, $a \prec b$ se $a - b$ é par.

f) Em $\mathbb{Z}$, $a \prec b$ se $|a| \leq |b|$.

g) Em $\mathbb{Z}$, $a \prec b$ se $a^2 \leq b^2$.