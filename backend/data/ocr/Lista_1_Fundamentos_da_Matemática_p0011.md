h) Em $\mathbb{Z}$, $a \prec b$ se $a + b \geq 0$.

i) Em $\mathbb{N}$, $a \prec b$ se $\text{mmc}(a, b) = a$.

j) Em $\mathbb{N}$, $a \prec b$ se $\text{mdc}(a, b) = 1$.

k) Em $\mathcal{P}(X)$, $A \prec B$ se $A \subseteq B$.

**Exercício 42.** Considere o conjunto

$$X = \{1, 2, 3, 4, 6, 12\}$$

com a relação de divisibilidade.

a) Verifique que essa relação define uma ordem parcial em $X$.

b) Determine quais pares de elementos são comparáveis.

c) Desenhe o diagrama de Hasse desse conjunto ordenado.

**Exercício 43.** Considere o conjunto

$$\mathcal{P}(\{a, b, c\}) = \{\emptyset, \{a\}, \{b\}, \{c\}, \{a, b\}, \{a, c\}, \{b, c\}, \{a, b, c\}\}$$

com a relação de inclusão.

a) Verifique que essa relação define uma ordem parcial em $\mathcal{P}(\{a, b, c\})$.

b) Determine quais pares de elementos são comparáveis.

c) Desenhe o diagrama de Hasse desse conjunto ordenado.

**Exercício 44.** Em cada item, determine as cotas superiores e inferiores com relação à ordem $\leq$, e diga se o conjunto possui máximo e mínimo.

a) $\mathbb{N}$

b) $\mathbb{Z}$

c) $\left\{\frac{1}{n+1}: n \in \mathbb{N}\right\}$

d) $\{n \in \mathbb{Z}: n^2 \leq 10\}$

e) $\{n \in \mathbb{Q}: n^2 \leq 10\}$

f) $\{n \in \mathbb{R}: n^2 \leq 10\}$

g) $(0, 1)$

h) $(0, 1]$

i) $[0, 1)$

**Exercício 45.** Sejam os conjuntos

$$X = \{1, 2, 3, 6, 12\}, \quad Y = \{2, 3, 4, 6, 18\}, \quad Z = \{0, 1, 2, 3, 5\}, \quad W = \mathbb{N} \cup \{0\}, \quad V = \mathbb{N} \setminus \{1\}$$

com a relação de divisibilidade.