"use client";
import type { DayView } from "@/lib/types";
interface Props { courseId: number; days: DayView[]; }
// Real implementation in Task 22.
export function NextUpPanel(_props: Props) { return null; }
